overwrite: true
schema: "../server/gql_api/schema.graphql"
documents: "api/**/*.graphql"
generates:
  api/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"

config:
  namingConvention: keep