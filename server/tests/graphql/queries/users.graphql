query users {
    users {
        id
        firstName
        lastName
        roles {
            title
            priority
        }
        computers {
            id
            name
        }
    }
}